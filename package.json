{
  "name": "soundproof",
  "version": "0.1.0",
  "description": "SoundProof — réservation de salles de répétition (monorepo : API NestJS + app mobile Expo)",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.9.5"
  },
  "lint-staged": {
    "*.{json,md,yml,yaml}": "prettier --write"
  }
}
